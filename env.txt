# ====================================================================
# VAULT ASSET LEDGER & BUDGET ENVIRONMENT VARIABLES
# ====================================================================

# --------------------------------------------------------------------
# 1. AI ASSISTANT (Google Gemini API)
# --------------------------------------------------------------------
# Required for AI financial assistant and smart budgeting features.
# In Google AI Studio, this is managed via the Secrets panel.
GEMINI_API_KEY=""

# --------------------------------------------------------------------
# 2. APPLICATION HOSTING
# --------------------------------------------------------------------
# The URL where this applet or server is hosted.
# Example for local development: http://localhost:3000
# Example for production: https://your-domain.com
APP_URL="http://localhost:3000"

# Server Port (Defaults to 3000)
PORT=3000

# Environment mode: "development" or "production"
NODE_ENV="development"

# --------------------------------------------------------------------
# 3. SECURITY & SESSION TOKENS
# --------------------------------------------------------------------
# Cryptographic secret key used to sign and encrypt session tokens / cookies.
# Generate a secure 32+ character random string (e.g. `openssl rand -hex 32`).
SESSION_SECRET=""

# --------------------------------------------------------------------
# 4. DATABASE CONFIGURATION (Supabase)
# --------------------------------------------------------------------
# Your Supabase Project URL (e.g., https://xyzcompany.supabase.co)
VITE_SUPABASE_URL=""
SUPABASE_URL=""

# Your Supabase Public Anonymous API Key (anon public key)
VITE_SUPABASE_ANON_KEY=""
SUPABASE_ANON_KEY=""

# (Optional) Supabase Service Role Key for elevated backend tasks
SUPABASE_SERVICE_ROLE_KEY=""

# --------------------------------------------------------------------
# 5. EMAIL DELIVERY / SMTP (For OTP Verification)
# --------------------------------------------------------------------
# SMTP Host (e.g., smtp.gmail.com, smtp.sendgrid.net, smtp.mailgun.org)
SMTP_HOST=""

# SMTP Port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# SMTP User / Email account
SMTP_USER=""

# SMTP Password or App Password (e.g. Google 16-character App Password)
SMTP_PASS=""

# Default Sender display email (e.g. "Vault Ledger <no-reply@yourdomain.com>")
SMTP_FROM=""
